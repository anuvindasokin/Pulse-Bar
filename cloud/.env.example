PORT=10000
PUBLIC_BASE_URL=https://your-pulsebar-connect.onrender.com
META_APP_ID=
META_APP_SECRET=
META_GRAPH_VERSION=v23.0
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
STATE_SECRET=
DATA_ENCRYPTION_KEY=
# Map each sellable device ID to its unique secret. Never commit real values.
DEVICE_KEYS={"pulsebar-demo":"replace-with-a-long-random-device-secret"}
DATA_FILE=/var/data/pulsebar-connect.json
