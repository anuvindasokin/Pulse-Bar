services:
  - type: web
    name: pulsebar-interface-demo
    runtime: python
    plan: free
    buildCommand: echo "No build required"
    startCommand: python tools/preview_server.py --host 0.0.0.0
    healthCheckPath: /health
    autoDeploy: true
